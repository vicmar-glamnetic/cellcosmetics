<div class="account-wrapper">
    <div class="account account-register">
        <div class="account-header">
            <h1 class="title">{{ 'customer.register.title' | t }}</h1>
        </div>
        <div class="account-body">
            <div id="CustomerRegisterForm">
            {% form 'create_customer' %}
                <div class="fieldset name">
                    <div class="field col">
                        <div class="input-wrapper">
                            <input type="text"
                                name="customer[first_name]"
                                id="FirstName"
                                class="required" 
                                placeholder="{{ 'customer.register.first_name' | t }}"
                                autofocus
                                {% if form.first_name %}value="{{ form.first_name }}"{% endif %}>
                            <label for="FirstName" >{{ 'customer.register.first_name' | t }}</label>
                        </div>
                    </div>
                    <div class="field col">
                        <div class="input-wrapper">
                            <input type="text"
                                name="customer[last_name]"
                                id="LastName"
                                class="required" 
                                placeholder="{{ 'customer.register.last_name' | t }}"
                                {% if form.last_name %}value="{{ form.last_name }}"{% endif %}>
                            <label for="LastName" >{{ 'customer.register.last_name' | t }}</label>
                        </div>
                    </div>
                </div>
                <div class="fieldset">
                    <div class="field">
                        <div class="input-wrapper {% if form.errors contains 'email' %}input-error{% endif %}">
                            <input type="email"
                                name="customer[email]"
                                id="CreateEmail"
                                class="required {% if form.errors contains 'email' %}input-error{% endif %}"
                                placeholder="{{ 'customer.register.email' | t }}"
                                value="{% if form.email %}{{ form.email }}{% endif %}"
                                spellcheck="false"
                                autocomplete="off"
                                autocapitalize="off">
                            <label for="Email" >{{ 'customer.register.email' | t }}</label>
                        </div>
                    </div>
                    <div class="field">
                        <div class="input-wrapper {% if form.errors contains 'password' %}input-error{% endif %}">
                            <input type="password"
                                name="customer[password]"
                                id="CreatePassword"
                                class="required {% if form.errors contains 'password' %}input-error{% endif %}"
                                placeholder="{{ 'customer.register.password' | t }}"
                                spellcheck="false"
                                autocomplete="off"
                                autocapitalize="off" >
                            <label for="CreatePassword" >{{ 'customer.register.password' | t }}</label>
                        </div>
                    </div>
                </div>
                <div class="fieldset dob">
                    <div class="hide" aria-hidden="true">
                        <input type="text" id="Dob" name="customer[note][DOB]" tabindex="-1" value="" disabled="disabled">
                    </div>
                    <div class="field col">
                        <div class="input-wrapper">
                            <input type="number"
                                name="customer[dob_month]"
                                id="DobMonth"
                                length="2"
                                placeholder="{{ 'customer.register.dob_month' | t }}"
                                value="" >
                        </div>
                    </div>
                    <div class="field col">
                        <div class="input-wrapper">
                            <input type="number"
                                name="customer[dob_day]"
                                id="DobDay"
                                length="2"
                                placeholder="{{ 'customer.register.dob_day' | t }}"
                                value="" >
                        </div>
                    </div>
                </div>
                <div class="fieldset newsletter">
                    <div class="field">
                        <div class="input-wrapper">
                            <input type="checkbox"
                                name="customer[accepts_marketing]"
                                id="AcceptMarketing"
                                value="1" >
                            <label for="AcceptMarketing">{{ 'customer.register.newsletter' | t }}</label>
                        </div>
                    </div>
                </div>
                
                {{ form.errors | default_errors }}
                
                <div class="actions">
                    <button type="submit" class="btn">{{ 'customer.register.submit' | t }}</button>
                </div>
            {% endform %}
            </div>
            <div class="additional-content">
                REMINDER: If you are re-registering your account, please make sure to use the same email address already associated with your account to maintain account-specific information.
            </div>
        </div>
        <div class="account-footer">
            <h1 class="title">{{ 'customer.register.login_title' | t }}</h1>
            <a class="btn cta-2" href="{{ routes.account_login_url }}">{{ 'customer.register.login_button' | t }}</a>
        </div>
    </div>
</div>